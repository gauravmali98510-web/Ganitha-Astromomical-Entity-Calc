#pragma once

#include "houseCounter.h"
class aarudh {

public:
	string findArudha(planet p_planet, string sclord, string aqlord);
	int rashicounter(int from, int to);
	int A_bhavatbhavam(int rashinum,int howmany);
	string arudhReport();
	string alsign(string sign_);
	string ALlordYog(sign* al);
	void outputdoc(int imgno);
};
